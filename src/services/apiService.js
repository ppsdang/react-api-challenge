// apiService.js
const BASE_URL = 'https://reqres.in/api';

export const login = (username, password) => {

};

export const fetchResources = () => {
    
};

export const fetchResourceDetail = (resourceId) => {
    
};
